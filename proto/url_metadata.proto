syntax = "proto3";

package url_metadata;

service UrlMetadata {
    rpc getLastUpdatedDateTime(UrlMetadataRequest) returns (lastUpdatedDateResponse) {}
}

message UrlMetadataRequest {
    string domain = 1;
    string articleTitle = 2;
}

message lastUpdatedDateResponse {
    string lastIndexedDate = 1;
}
